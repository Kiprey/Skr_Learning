class A {

  a: Int <- 0;
  d: Int <- 1;
  f(): Int { a <- a + d }; 
  
};

class B inherits A {
  b: Int <- 2;
  f(): Int {a };
  g(): Int { a <- a - b };
};

class C inherits A {
   c: Int <- 3;
   h(): Int { a <- a - c };
}; 
class Main { main () : Int { 6 }; };
